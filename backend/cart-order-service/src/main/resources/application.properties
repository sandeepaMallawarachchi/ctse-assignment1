spring.application.name=cart-order-service
server.port=8084

# MongoDB
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://sdilrangi58_db_user:ksdk1357%40A@cluster0.svbsbni.mongodb.net/cart_order_db?appName=Cluster0}

# JWT (must match the secret used by Auth Service — same env var JWT_SECRET_BASE64)
jwt.secret=${JWT_SECRET_BASE64:U3VwZXJTZWNyZXRLZXlTdXBlclNlY3JldEtleVN1cGVyU2VjcmV0S2V5MTIzNDU2}

# External Service URLs
service.product-catalog.url=${PRODUCT_CATALOG_URL:http://localhost:8081}
service.payment.url=${PAYMENT_SERVICE_URL:http://localhost:8083}
service.gateway.url=${API_GATEWAY_URL:http://localhost:8080}

# Self URL (used for payment callback registration)
service.self.url=${SELF_URL:http://localhost:8084}

# Internal service-to-service key (used by Payment Service callback)
service.internal-key=${INTERNAL_SERVICE_KEY:ctse-internal-secret-2024}

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.info.env.enabled=true

# App Info
info.app.name=Cart & Order Service
info.app.version=1.0.0
info.app.description=Manages shopping carts and orders

# Logging
logging.level.com.ctse=DEBUG
logging.level.org.springframework.security=INFO

# SpringDoc / Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.disable-swagger-default-url=true
